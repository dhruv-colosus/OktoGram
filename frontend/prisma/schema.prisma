generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  email    String @id
  username String @unique
  wallet   String
}

model Image {
  id      String @id @default(uuid())
  content String
}
